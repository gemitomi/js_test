var name;
const person = {
    
    introduce: function(name) {
      console.log("Hello, a nevem " + person.name);
    },
  };
  person.introduce("Berry"); // logs “Hello, a nevem Berry”
  person.introduce("Henry");
  person.introduce("Lerry");

   